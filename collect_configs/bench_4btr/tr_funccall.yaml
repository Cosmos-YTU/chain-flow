# Turkish HOLDOUT for the 4B drafter -- conversation-disjoint from the stage1_4btr train split
# (build_turkish_prompts.py groups by primary_id before splitting). Same `pretemplated` handler
# and same non-thinking prefix as training, so eval is byte-identical to collection.
model_id: Qwen/Qwen3.5-4B
dataset_name: json
data_files: bench_data_tr/tr-function-calling-20k.holdout.jsonl
dataset_config: null
split: train
source: tr_funccall
format_name: pretemplated
generation_max_new_tokens: 256
generation_batch_size: 64
hidden_batch_size: 24
storage_dtype: float16
dtype: float16
device: cuda:0
output_dir: teacher_states/bench-4btr-tr_funccall
