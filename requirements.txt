# chained-flow -- TRAINING environment (CUDA 13).
#
#   uv venv --python 3.12
#   uv pip install -r requirements.txt
#
# Exact versions the shipped Turkish drafters were trained with. Pinned rather than ranged
# because a drafter is judged against numbers measured under one environment, and a torch or
# transformers minor bump moves accept by more than the effects we routinely act on.
#
# vLLM is deliberately ABSENT: training imports it nowhere (the only vllm imports are under
# src/chained_flow/vllm_tree/, the serving path). Installing it drags in a second torch pin and
# is the biggest source of environment conflict here. Serve from a SEPARATE venv --
# `uv pip install chained-flow` -- see README.

--extra-index-url https://download.pytorch.org/whl/cu130

torch==2.12.0+cu130
triton==3.7.0

transformers==5.9.0
tokenizers==0.22.2
accelerate==1.13.0
datasets==4.8.5
safetensors==0.7.0
huggingface-hub==1.17.0
hf-xet==1.5.0

numpy==2.4.6
PyYAML==6.0.3
tqdm==4.67.3
python-dotenv==1.2.2
